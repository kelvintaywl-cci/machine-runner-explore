version: 2.1

executors:
  machine3-linux:
    machine: true
    resource_class: kelvintaywl-cci/machine3-linux-tf

jobs:
  foo:
    executor: machine3-linux
    steps:
      # 5 mins
      - run: sleep 300
  bar:
    executor: machine3-linux
    steps:
      - run: sleep 30

workflows:
  main:
    jobs:
      - foo
      - bar
