version: 2.1

executors:
  machine3-linux:
    machine: true
    resource_class: kelvintaywl-cci/lightsail-medium

jobs:
  foo:
    executor: machine3-linux
    steps:
      - run: whoami
      - run: cowsay -f tux 'I love Linux!'

workflows:
  main:
    jobs:
      - foo:
          context:
            - foo-2
