version: 2.1

executors:
  machine3-linux:
    machine: true
    resource_class: kelvintaywl-cci/lightsail-medium-throwaway

jobs:
  foo:
    executor: machine3-linux
    steps:
      - run: whoami
      - run: printenv PATH


workflows:
  main:
    jobs:
      - foo:
          context:
            - foo-2
