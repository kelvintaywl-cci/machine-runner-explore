version: 2.1

jobs:
  test:
    machine: true
    resource_class: kelvintaywl-cci/ec2-linux2023-tf
    environment:
    steps:
      - run:
          name: create file
          command: |
            # 1G file
            head -c 1073741824 </dev/urandom >/tmp/$CIRCLE_WORKFLOW_JOB_ID.dump

workflows:
  main:
    jobs:
      - test
